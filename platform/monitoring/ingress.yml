# cluster-specific: NETWORK_HOSTNAME_SUFFIX rendered as themartinez.cloud
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: monitoring-http
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`monitoring.themartinez.cloud`) && PathPrefix(`/prometheus`)
      services:
        - name: monitoring-kube-prometheus-prometheus
          port: 9090
      middlewares:
        - name: stripprefix
    - kind: Rule
      match: Host(`monitoring.themartinez.cloud`) && PathPrefix(`/grafana`)
      services:
        - name: monitoring-grafana
          port: 80
