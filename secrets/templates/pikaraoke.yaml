---
# Pushed by scripts/sync-secrets.sh via `op inject`. 1Password item "pikaraoke".
apiVersion: v1
kind: Secret
metadata:
  name: pikaraoke-secret
  namespace: pikaraoke
type: Opaque
stringData:
  PIKARAOKE_ADMIN_PASSWORD: "{{ op://$OP_VAULT/pikaraoke/PIKARAOKE_ADMIN_PASSWORD }}"
