---
# Pushed by scripts/sync-secrets.sh via `op inject`. 1Password item "nebulasync".
apiVersion: v1
kind: Secret
metadata:
  name: nebulasync-secret
  namespace: nebulasync
type: Opaque
stringData:
  PRIMARY: "{{ op://$OP_VAULT/nebulasync/PRIMARY }}"
  REPLICAS: "{{ op://$OP_VAULT/nebulasync/REPLICAS }}"
