auth:
  adminUser: admin
  adminPassword: ""  # moved to keycloak-secret via ExternalSecret
replicaCount: 1
service:
  type: ClusterIP

postgresql:
  enabled: false

externalDatabase:
  host: 10.43.100.50
  user: rpi # cluster-specific: POSTGRES_USER
  password: 
  database: keycloak
  existingSecret: ""
  port: 5432

ingress:
  enabled: true
  ingressClassName: nginx
  hostname: keycloak.home.arpa # cluster-specific: NETWORK_HOSTNAME_SUFFIX
  tls: false                # Or true + cert-manager config
  path: /
