apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: meal-planner-ingress
  annotations:
    cluster-config/suffix-host: "true"
    cluster-config/suffix-tls: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: security-redirect-https@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: meals.SUFFIX
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: meal-planner-svc
                port:
                  name: 80-tcp
  tls:
    - secretName: meal-planner-tls
      hosts:
        - meals.SUFFIX
