apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: uptime-pdb
  # Single replica by design (SQLite/local storage); PDB prevents accidental eviction
spec:
  minAvailable: 1
  selector:
    matchLabels: {}
